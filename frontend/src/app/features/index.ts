import { Features } from './config/types'
import { featuresConfig } from './config'

// access helpers for feature names
export const featureNameMeasurements = featuresConfig[Features.Measurements].name
